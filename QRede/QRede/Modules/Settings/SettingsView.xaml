<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:QRede.Modules"
             xmlns:controls="clr-namespace:QRede.CustomControls"
             xmlns:resources="clr-namespace:QRede.Language"
             x:DataType="vm:SettingsViewModel"
             mc:Ignorable="d"
             x:Class="QRede.Modules.SettingsView">

    <Grid Margin="10" RowDefinitions="auto,auto, auto" RowSpacing="15" >
        <Label Text="{x:Static resources:Language.Theme}" d:Text="Tema"/>
        <Label Text="{Binding ThemeText}" Grid.Row="1" d:Text="Switch" Margin="25,0,0,0"/>
        <controls:CustomSwitch IsToggled="{Binding IsDarkTheme}" Grid.Row="1" HorizontalOptions="End"/>

        <Label Text="{x:Static resources:Language.About}" d:Text="placeholder" Grid.Row="2" >
            <Label.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding OpenAboutCommand}"/>
            </Label.GestureRecognizers>
        </Label>
    </Grid>
</ContentPage>